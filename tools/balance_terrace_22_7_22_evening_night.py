from pathlib import Path
# KONO 22.7.22 output is baked into the 24 full-map PNGs.
# Authoring parameters used: Evening saturation x0.80/value x0.98 (warm practical cores preserved);
# Night saturation x0.80/value x0.985. Morning and Afternoon were locked byte-identical to 22.7.21.
# Runtime performs no terrace tint, overlay, glow, or recolor.
print("KONO 22.7.22 terrace balance is already baked into production PNGs.")
