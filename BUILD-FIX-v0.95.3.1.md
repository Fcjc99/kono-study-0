# KONO v0.95.3.1 Build Fix

Fixed Phaser 4 TypeScript compatibility in `Stage0Scene.ts` by removing the obsolete `add` property from generated graphics configuration objects.

Affected generated textures:
- lantern glow
- rain drop
- snow flake
- cloud soft
